<!DOCTYPE html>
<html lang="en">
    <head>
        <!-- head/meta goes here -->
        {% include "./_parts/head.njk" %}
        <!-- // -->
    </head>
    <body>
        <header>
            <!-- Header goes here -->
            {% include "./_parts/header.njk" %}
            <!-- // -->
        </header>
        <main id="qa-content">
            <h1 class="qa-title">{{ title }}</h1>
            <div class="qa-body">
                {{ content | safe }}
            </div>
            <div class="qa-buttons">
                <button onclick="location.href='{{ button1.url }}'">{{ button1.text }}</button>
                <button onclick="location.href='{{ button2.url }}'">{{ button2.text }}</button>
            </div>
        </main>
        <footer>
            <!-- Footer goes here -->
            {% include "./_parts/footer.njk" %}
            <!-- // -->
        </footer>
    </body>
<style>
    body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        background-color: #f0f0f0;
    }
    .qa-title {
        text-align: center;
        margin-top: 20px;
        font-size: 2rem;
        color: #333;
    }
    .qa-body {
        margin: 20px 0;
        padding: 20px;
        background-color: #fff;
        border-radius: 8px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    }
    .qa-buttons {
        display: flex;
        justify-content: center;
        gap: 10px;
    }
    .qa-buttons button {
        padding: 10px 20px;
        font-size: 1rem;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        background-color: #007bff;
        color: white;
        transition: background-color 0.3s;
    }
    .qa-buttons button:hover {
        background-color: #0056b3;
    }
</style>
</html>